//json: Es un formato para el intercambio de datos y un subconjunto de los objetos
var miobj = {
        nombre: "rodrigo"
    }
    // JSON.stringify te da el formato en json.
console.log(JSON.stringify(miobj));

// para ver una api visitar:   https://yts.mx/api
// si se quiere ver los formatos de los json
// ver  https://yts.mx/api/v2/list_movies.json